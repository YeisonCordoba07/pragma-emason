server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost/emason4
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      springframework:
        security: TRACE